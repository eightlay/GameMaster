{
    "users": "CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY, active INT);",
    "profiles": "CREATE TABLE IF NOT EXISTS profiles (id INTEGER PRIMARY KEY, name TEXT, age INTEGER, about TEXT, bio TEXT, prefs TEXT, FOREIGN KEY (id) REFERENCES users (id));"
}